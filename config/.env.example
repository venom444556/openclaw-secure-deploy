# =============================================================
# PGPClaw — Environment Variables (Non-Secret Only)
#
# ALL SECRETS are stored in OpenBao, NOT here.
# Run: ./openbao/scripts/store-secret.sh <name> <value>
# =============================================================

# ── Ports (non-secret) ──────────────────────────────────────
OPENCLAW_GATEWAY_PORT=18789

# ── OpenBao ─────────────────────────────────────────────────
BAO_ADDR=http://127.0.0.1:8200

# ── Profile ─────────────────────────────────────────────────
# core       — OpenBao + OpenClaw gateway
# monitoring — adds Prometheus, Grafana, Alertmanager, n8n
# oauth      — adds Nango OAuth proxy
# full       — everything
PGPCLAW_PROFILE=core

# ── Local AI (Ollama) ───────────────────────────────────────
OLLAMA_HOST=http://localhost:11434

# ── Optional: Slack webhook for alerts (non-secret config) ──
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# ── Optional: Email for cost alerts ─────────────────────────
# ALERT_EMAIL=your@email.com
